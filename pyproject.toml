[project]
name = "research-system"
version = "0.1.0"
description = "Source-based briefing research system schemas."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2,<3",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
